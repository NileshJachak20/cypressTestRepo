{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "Cypress test using cucumber",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --spec \"**/*.feature\"",
    "cypress:open": "./node_modules/.bin/cypress open",
    "cypress:run": "./node_modules/.bin/cypress run -- --spec='cypress/integration/features/*'"
  },
  "author": "Nilesh jachak",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0"
  },
  "dependencies": {
    "cypress": "^4.9.0",
    "cypress-cucumber-preprocessor": "^2.5.0"
  }
}
